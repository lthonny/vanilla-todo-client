import { createTaskSwitch } from './toggleDOM';
import { createTaskText } from './messageDOM';
import { createEditText } from './editMessageDOM';
import { createDeleteBtn } from './deleteBtnDOM';
import { modalWindow } from './modalWindowDOM';

export const DOMElements = {
  createTaskSwitch,
  createTaskText,
  createEditText,
  createDeleteBtn,
  modalWindow
}

